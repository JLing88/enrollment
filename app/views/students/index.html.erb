<h1>All Students</h1>

<% @students.each do |student| %>
<ul>
  <li id="student-<%= student.id %>"> <%= student.name %> <%= link_to "Delete", student_path(student), method: :delete %> </li>
</ul>
<% end %>
